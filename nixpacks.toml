# Nixpacks configuration for Railway deployment
# Ensures Node.js 22 LTS is used for eslint-visitor-keys compatibility

[phases.setup]
nixPkgs = ["nodejs_22", "npm-10_x"]

[phases.build]
cmds = ["npm ci", "npm run build"]

[start]
cmd = "npm run start"
