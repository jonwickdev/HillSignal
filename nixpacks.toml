# Nixpacks configuration for Railway deployment
# Keep it simple - let Railway handle caching

[phases.setup]
nixPkgs = ["nodejs_22", "npm-10_x"]

[phases.build]
cmds = ["npm ci", "npm run build"]

[start]
cmd = "npm run start"
